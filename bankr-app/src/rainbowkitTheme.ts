import { darkTheme } from "@rainbow-me/rainbowkit";

/** Matches site tokens in index.css (black / white / blue). */
export const marketplaceTheme = darkTheme({
  accentColor: "#4da3ff",
  accentColorForeground: "#f2f4f8",
  borderRadius: "medium",
  fontStack: "system",
  overlayBlur: "small",
});
